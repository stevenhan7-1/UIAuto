import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {

  await page.getByRole('textbox', { name: '* 施工任务 :' }).click();
  await page.getByRole('textbox', { name: '* 施工任务 :' }).fill('试件制作');
  await page.getByRole('textbox', { name: '* 施工任务 :' }).press('Enter');
  await page.locator('div:nth-child(15) > .ant-tree-checkbox > .ant-tree-checkbox-inner').click();
  await page.getByRole('button', { name: '完 成' }).click();
});